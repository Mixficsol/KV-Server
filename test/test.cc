#include <vector>
#include <string>
#include <iostream>

int main() {
  std::vector<std::string> v;
  v[0] = "test";
  vector<std::string>::iterator iter = v1.begin();
  for (; iter != v1.end(); ++iter) {
    cout << *iter << " ";
  }
 // std::cout << v[0] << std::endl;
  return 0;
}
