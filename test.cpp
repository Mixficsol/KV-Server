#include <iostream>
#include <stdio.h>
#include <assert.h>

using namespace std;

char str[1010] = {};

int main() {
  scanf("%s", &str);
  int i = 0;
  assert(i > 1);
  cout << str << endl;
  return 0;
}
